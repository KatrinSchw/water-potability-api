{
  "devDependencies": {
    "@types/react": "^17.0.14",
    "@types/react-dom": "^17.0.9",
    "popper": "^1.0.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "dependencies": {
    "bootstrap": "^5.0.2",
    "jquery": "^3.6.0",
    "parcel": "^2.0.0-beta.2",
    "parcel-bundler": "^1.12.5",
    "react-bootstrap": "^1.6.1",
    "typescript": "^4.3.5"
  },
  "scripts": {
    "dev": "parcel index.html --out-dir build/debug",
    "deploy": "yarn upgrade && yarn install && yarn build",
    "build": "echo `pwd` && parcel build index.html --out-dir build/release --public-url ./",
    "start": "parcel index.html --out-dir build"
  }
}
