{
  "name": "frontend",
  "version": "1.0.0",
  "devDependencies": {
    "@types/react": "^17.0.14",
    "@types/react-dom": "^17.0.9",
    "parcel-bundler": "^1.12.5",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "dependencies": {
    "bootstrap": "^5.0.2",
    "react-bootstrap": "^1.6.1",
    "typescript": "^4.3.5"
  },
  "scripts": {
    "dev": "parcel index.html --out-dir build/debug",
    "deploy": "yarn install && yarn build",
    "build": "echo `pwd` && parcel build index.html --out-dir build --public-url ./",
    "start": "parcel index.html --out-dir build"
  }
}
